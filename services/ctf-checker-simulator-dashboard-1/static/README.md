# ctf-checker-simulator-dashboard-1

This report was generated by deterministic file, metadata, and pattern inspection. Pattern matches require manual review.

## Service overview

| Field | Value |
|---|---|
| Container | ctf-checker-simulator-dashboard-1 (`f97e5190eea0`) |
| Image | `ctf-checker-simulator-dashboard` |
| Type | binary/network service |
| Languages | Python (2), JavaScript (1), HTML/templates (1) |
| Ports | 0.0.0.0:8080 → 8080/tcp, :::8080 → 8080/tcp |
| Files in captured trees | 8 |
| Files selected for application analysis | 8 |
| Readable / binary selected files | 8 / 0 |
| Running processes | 1 |
| Compose subservices | 1 |
| Databases | SQLite |
| Technology | socat |

## Likely challenge classes

These are deterministic hypotheses formed by combining source/sink categories. Review the cited evidence before changing code.

| Severity | Confidence | Possible challenge | Why | Evidence |
|---|---|---|---|---|
| critical | medium | Prototype pollution | Prototype pollution primitive | Prototype pollution primitive: source/app.py:181 |
| critical | medium | SQL injection | SQL string construction | SQL string construction: source/static/app.js:7; SQL string construction: source/static/app.js:8; SQL string construction: source/static/app.js:9 |
| critical | medium | Secret recovery / hardcoded flag | Flag-like literal | Flag-like literal: source/app.py:127 |
| high | medium | Authentication/JWT logic flaw | Authorization decision | Authorization decision: source/README.md:36; Authorization decision: source/app.py:84; Authorization decision: source/app.py:134 |
| high | high | Broken object authorization / IDOR | Direct object identifier | Direct object identifier: source/static/app.js:2; Authorization decision: source/README.md:36; Authorization decision: source/app.py:84; Authorization decision: source/app.py:134; Raw SQL execution: source/app.py:44; Raw SQL execution: source/app.py:74; Raw SQL execution: source/app.py:76; SQL query: source/app.py:255; SQL query: source/app.py:260; SQL query: source/app.py:276 |
| high | medium | Cross-site scripting | Potential XSS sink | Potential XSS sink: source/static/app.js:9; Potential XSS sink: source/static/app.js:10; Potential XSS sink: source/static/app.js:11 |
| high | medium | Server-side request forgery | SSRF-capable request | SSRF-capable request: source/app.py:12; SSRF-capable request: source/app.py:85; SSRF-capable request: source/app.py:87 |

## Runtime and configuration

- Command: `python app.py`
- Working directory: `/app`
- Container user: `65534:65534`
- Compose project/service: `ctf-checker-simulator` / `dashboard`
- Captured archives: 0
- Environment variable names: CHECK_INTERVAL, DB_PATH, GPG_KEY, PATH, PYTHON_SHA256, PYTHON_VERSION, TARGET_HOST, WEB_PORT

### Mounts

| Type | Source | Destination | Writable |
|---|---|---|---|
| volume | `/var/lib/docker/volumes/ctf-checker-simulator_simulator-data/_data` | `/data` | True |

### Docker networks

| Network | Container IP | Aliases |
|---|---|---|
| ctf-checker-simulator_default | `172.20.0.2` | ctf-checker-simulator-dashboard-1, dashboard |

### Configuration attention

- Container root filesystem is writable.
- Writable volume at /data

## Accessible routes

No routes were recognized by the static patterns.

## Native binary specifications

No ELF application binaries were found.

### Other binary or compiled artifacts

No non-text application artifact was selected.

## Mounted data file inventory

No files were captured from data-only mounts.

## Package and build manifests

No recognized package or build manifest was found in the selected application trees.

## Interesting patterns

| Severity | Category | Meaning | Location | Matched line |
|---|---|---|---|---|
| medium | Authorization decision | Role, ownership, or login state is checked | `source/README.md:36` | `The attack routines are tailored to this training lab: PHP and Java object authorization,` |
| high | SSRF-capable request | An outbound request API is used | `source/app.py:12` | `import urllib.request` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:44` | `db.execute('PRAGMA journal_mode=WAL')` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:74` | `columns = {row[1] for row in db.execute('PRAGMA table_info(results)')}` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:76` | `db.execute("ALTER TABLE results ADD COLUMN stolen_flags TEXT NOT NULL DEFAULT '[]'")` |
| medium | Authorization decision | Role, ownership, or login state is checked | `source/app.py:84` | `headers['Authorization'] = 'Bearer ' + token` |
| high | SSRF-capable request | An outbound request API is used | `source/app.py:85` | `request = urllib.request.Request(url, data=payload, headers=headers, method=method)` |
| high | SSRF-capable request | An outbound request API is used | `source/app.py:87` | `with urllib.request.urlopen(request, timeout=6) as response:` |
| critical | Flag-like literal | A CTF flag-like literal appears in source or configuration | `source/app.py:127` | `key = f'flag{nonce}'` |
| medium | Authorization decision | Role, ownership, or login state is checked | `source/app.py:134` | `if replies[1] != 'OK authenticated' or replies[2] != 'OK stored' or replies[3] != 'VALUE ' + flag:` |
| medium | Authorization decision | Role, ownership, or login state is checked | `source/app.py:151` | `if replies[0] != 'OK authenticated' or replies[1] != 'VALUE ' + placement['flag']:` |
| medium | Authorization decision | Role, ownership, or login state is checked | `source/app.py:181` | `http_json('web-node', '/api/profile', 'PATCH', {'__proto__': {'isAdmin': True}}, token)` |
| critical | Prototype pollution primitive | Prototype keys, unsafe recursive merge, or object-path mutation appears | `source/app.py:181` | `http_json('web-node', '/api/profile', 'PATCH', {'__proto__': {'isAdmin': True}}, token)` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:255` | `row = db.execute('SELECT username,password,item_key,flag FROM placements WHERE service=?', (service,)).fetchone()` |
| info | SQL query | SQL operation is present | `source/app.py:255` | `row = db.execute('SELECT username,password,item_key,flag FROM placements WHERE service=?', (service,)).fetchone()` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:260` | `db.execute('''INSERT INTO placements(service,username,password,item_key,flag) VALUES(?,?,?,?,?)` |
| info | SQL query | SQL operation is present | `source/app.py:260` | `db.execute('''INSERT INTO placements(service,username,password,item_key,flag) VALUES(?,?,?,?,?)` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:264` | `db.execute('''INSERT OR IGNORE INTO placement_history(service,username,item_key,flag)` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:276` | `tick_id = db.execute('INSERT INTO ticks(started_at,target) VALUES(?,?)', (now(), TARGET)).lastrowid` |
| info | SQL query | SQL operation is present | `source/app.py:276` | `tick_id = db.execute('INSERT INTO ticks(started_at,target) VALUES(?,?)', (now(), TARGET)).lastrowid` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:300` | `db.execute('''INSERT INTO results(tick_id,service,sla_ok,steal_ok,previous_verified,` |
| info | SQL query | SQL operation is present | `source/app.py:300` | `db.execute('''INSERT INTO results(tick_id,service,sla_ok,steal_ok,previous_verified,` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:306` | `db.execute('UPDATE ticks SET finished_at=?,duration_ms=? WHERE id=?',` |
| info | SQL query | SQL operation is present | `source/app.py:306` | `db.execute('UPDATE ticks SET finished_at=?,duration_ms=? WHERE id=?',` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:321` | `ticks = [dict(row) for row in db.execute('SELECT * FROM ticks ORDER BY id DESC LIMIT 100')]` |
| info | SQL query | SQL operation is present | `source/app.py:321` | `ticks = [dict(row) for row in db.execute('SELECT * FROM ticks ORDER BY id DESC LIMIT 100')]` |
| medium | Raw SQL execution | A low-level SQL execution API is used | `source/app.py:323` | `tick['results'] = [dict(row) for row in db.execute('''SELECT service,sla_ok,steal_ok,` |
| info | SQL query | SQL operation is present | `source/app.py:323` | `tick['results'] = [dict(row) for row in db.execute('''SELECT service,sla_ok,steal_ok, previous_verified,latency_ms,stolen_count,stolen_flags,sla_error,steal_error FROM results WHERE tick_id=? ORDER BY service''', (tick['id'],))]` |
| medium | Direct object identifier | An identifier comes from a URL/query/request source | `source/static/app.js:2` | `const cards=document.querySelector('#cards'),history=document.querySelector('#history'),run=document.querySelector('#run');` |
| high | SQL string construction | SQL text is built with interpolation, formatting, or concatenation | `source/static/app.js:7` | `document.querySelector('#target').textContent=`Target ${data.target}`;` |
| high | SQL string construction | SQL text is built with interpolation, formatting, or concatenation | `source/static/app.js:8` | `document.querySelector('#clock').textContent=`Every ${data.interval}s · next in ${data.runtime.next_run_in??'…'}s`;` |
| high | Potential XSS sink | Unescaped HTML or DOM insertion sink appears | `source/static/app.js:9` | `const rt=document.querySelector('#runtime');rt.className='pill '+(data.runtime.running?'running':'');rt.innerHTML=`<span class="dot"></span>${data.runtime.running?'Tick running':'Scheduler active'}`;run.disabled=data.runtime.running;` |
| high | SQL string construction | SQL text is built with interpolation, formatting, or concatenation | `source/static/app.js:9` | `const rt=document.querySelector('#runtime');rt.className='pill '+(data.runtime.running?'running':'');rt.innerHTML=`<span class="dot"></span>${data.runtime.running?'Tick running':'Scheduler active'}`;run.disabled=data.runtime.running;` |
| high | Potential XSS sink | Unescaped HTML or DOM insertion sink appears | `source/static/app.js:10` | `const newest=data.ticks[0]?.results\|\|[];last=Object.fromEntries(newest.map(x=>[x.service,x]));cards.innerHTML='';` |
| high | Potential XSS sink | Unescaped HTML or DOM insertion sink appears | `source/static/app.js:11` | `for(const [id,meta] of Object.entries(data.services)){const r=last[id];const el=document.createElement('article');el.className='card';el.innerHTML=`<h2>${safe(meta.name)}</h2><div class="kind">${safe(id)} · :${meta.port} · ${safe(meta.attac` |
| high | Potential XSS sink | Unescaped HTML or DOM insertion sink appears | `source/static/app.js:12` | `if(!data.ticks.length)return;history.innerHTML='';` |
| high | Potential XSS sink | Unescaped HTML or DOM insertion sink appears | `source/static/app.js:13` | `for(const tick of data.ticks){const results=tick.results\|\|[],up=results.filter(r=>r.sla_ok).length,stolen=results.filter(r=>r.steal_ok).length,errors=results.filter(r=>r.sla_error\|\|r.steal_error).map(r=>`${r.service}: ${r.sla_error\|\|''}${r.` |
| high | SSRF-capable request | An outbound request API is used | `source/static/app.js:15` | `async function refresh(){try{const r=await fetch('/api/status',{cache:'no-store'});render(await r.json())}catch(e){document.querySelector('#runtime').textContent='Dashboard API unavailable'}}` |
| high | SSRF-capable request | An outbound request API is used | `source/static/app.js:16` | `run.onclick=async()=>{run.disabled=true;await fetch('/api/run',{method:'POST'});setTimeout(refresh,300)};` |

## Files needing attention

- `source/app.py` — attention score 49
- `source/static/app.js` — attention score 32
- `source/README.md` — attention score 2

## Decompiled artifacts

No Java or .NET artifact was decompiled.

## Embedded archive inspection

No selected source bundle was expanded.

## Application file inventory

- `source/.dockerignore`
- `source/.env.example`
- `source/Dockerfile`
- `source/README.md`
- `source/app.py`
- `source/compose.yaml`
- `source/static/app.js`
- `source/static/index.html`

The corresponding observations are in [the dynamic analysis report](../dynamic/README.md).

