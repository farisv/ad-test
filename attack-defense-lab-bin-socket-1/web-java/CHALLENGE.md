# Receipt Room

Keep registration, authentication, receipt creation/retrieval, and the welcome download.
Review object authorization, token trust, and path resolution. Existing user tokens do not
need to survive a rebuild, but all tokens issued after a rebuild must work.

