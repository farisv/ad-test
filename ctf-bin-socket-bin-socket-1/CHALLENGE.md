# Beacon Vault

The executable listens directly and forks a child per client. Preserve REGISTER, LOGIN,
PUT, and GET on one connection. Review credential comparison, privileged maintenance
access, and formatted output. The checker does not require MASTER, DUMP, or ECHO.
