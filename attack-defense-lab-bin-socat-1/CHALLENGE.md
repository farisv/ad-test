# Parcel Relay

`socat` launches a new copy of `service` for each TCP connection. Preserve REGISTER,
LOGIN, PUT, and GET on that same connection. Review memory handling in ECHO and how record
keys become filesystem paths. The checker does not call ECHO.

